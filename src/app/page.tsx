import MainPage from "./StartPage/StartPage";

export default function Home() {
  return (
    <main className="flex align-center items-center justify-center h-screen w-screen">
      <MainPage />
    </main>
  );
}
